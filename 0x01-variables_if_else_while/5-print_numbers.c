#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase, then in uppercase.
(*
 * Return: 0 on success
 */
int main(void)
{

	int c;

	for (c =0; c < 10; c++)
	{
		putchar(c);
	}
	
	putchar('\n');
	return (0);

}
